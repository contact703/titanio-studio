#!/bin/bash
MEMORY_FILE="/Volumes/TITA_039/MAC_MINI_03/.openclaw/workspace/MEMORY.md"
for topico in "$@"; do
    if grep -q "$topico" "$MEMORY_FILE" 2>/dev/null; then
        echo "[MEMORY-INJECT] $topico encontrado"
        return 0
    fi
done
return 1
