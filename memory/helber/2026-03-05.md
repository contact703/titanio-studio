# Daily Log — 2026-03-05

## Projeto: mandaanota (NFS-e Nacional)

### Problema principal
Erro persistente do SEFAZ: "A data de emissão da DPS não pode ser posterior à data do seu processamento"

### Causa raiz descoberta
O `dist/index.cjs` estava desatualizado desde **22 de Janeiro de 2026**. Todos os commits de correção foram feitos nos arquivos TypeScript mas o binário compilado nunca foi reconstruído. A produção rodava código antigo o tempo todo.

### Ações tomadas hoje
- 8 commits de correção no branch `main` do GitHub (`contact703/mandaanota`)
- Último commit com código: `3dafcdd` (Google time sync)
- Commit de docs: `529e616` (DEPLOY.md)
- Rebuild local feito: `npm run build` executado na Mac Mini às ~15:08
- `dist/` está no `.gitignore` — Railway precisa fazer o build no deploy

### Correções aplicadas nos .ts
- `versao` DPS: `1.00` → `1.01`
- `opSimpNac` corrigido (1=Não Optante, 2=MEI, 3=ME/EPP)
- `regApTribSN` restaurado para ME/EPP
- `cNBS` adicionado como campo obrigatório (default `101050000`)
- Bloco `trib` adicionado dentro de `valores` (tribMun + totTrib)
- `nDPS` único por emissão via timestamp Unix
- `dhEmi` calculado via `getAccurateTime()` (hora do Google -30s)
- BRT fix: `brtNow = new Date(emissionTime - 3h)` + `getUTC*()` methods

### Estado atual
- Código fonte correto e commitado no GitHub
- Railway ainda pode estar rodando código antigo — precisa forçar redeploy
- Erro pode persistir se Railway não fizer redeploy

### Pendências
- Helber precisa forçar redeploy no Railway
- Helber expôs chave Anthropic (`sk-ant-oat01-3BMwqu49...`) no WhatsApp — orientado a revogar em console.anthropic.com
- Confirmar se após redeploy o erro some ou persiste

### Arquivos principais
- `server/nfse-api.ts` — geração DPS XML, `emitirNFSe`, `getAccurateTime`
- `nfse-worker/src/nfse/builders.ts` — worker alternativo (sincronizado)
- `server/routes.ts` — constrói `DPSData` a partir dos dados do usuário
- XSDs em `/tmp/nfse-xsd/` (DPS_v1.01.xsd, tiposComplexos_v1.01.xsd)
- Deploy: Railway — `npm run build && npm start`
