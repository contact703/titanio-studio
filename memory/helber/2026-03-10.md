# 2026-03-10 — Memórias do Dia

## 🏆 VITÓRIA: Emissão NFS-e MEI 100% Funcional

### Conquista Principal
- **NFS-e MEI emitida com sucesso via automação!**
- Helber disse: "produto do ano com potencial de lucro imenso"
- Qualquer MEI do Brasil pode emitir nota fiscal por WhatsApp agora

### Problema Final Resolvido
- **Erro:** "Não é permitido o preenchimento das informações relativas aos tributos federais quando o emitente for identificado como MEI"
- **Causa:** Etapa 3 estava preenchendo dropdowns de PIS/COFINS e Tipo de Retenção
- **Fix:** Removido preenchimento de tributos federais para MEI (commit `3958774`)

### Outros Fixes do Dia
- `ab05f20`: Pula Município Prestador se disabled (evita timeout 30s)
- `5c0c211`: Botão Emitir é `#btnProsseguir` (link `<a>`, não button)
- `c9949ea`: Usa `expect_navigation` com click para garantir navegação real
- `8cd0eec`: Usa `page.goto(href)` para navegação confiável

### Fluxo Completo Funcionando
1. **ETAPA 1 (Pessoas):** Data, Município Prestador (auto), Tomador CPF/CNPJ
2. **ETAPA 2 (Serviço):** Município Prestação, cTribNac, Descrição, NBS
3. **ETAPA 3 (Valores):** Valor, NÃO preenche tributos federais (MEI isento)
4. **ETAPA 4 (Emitir):** Clica `#btnProsseguir` com `expect_navigation`

### Instabilidade Portal NFS-e
- Portal gov.br teve instabilidade ~11:51-11:58 (timeouts de 60s)
- API reconectou automaticamente quando portal voltou
- Keep-alive funcionando corretamente

### Projetos Relacionados
- **Backend API:** Railway `aplicativo-api-production-5163.up.railway.app`
- **Manda a Nota:** Railway `mandaanota`

### Próximos Passos (confirmados pelo Helber)
1. Corrigir NBS para correlacionar com descrição do serviço
2. Gerar/baixar PDF da nota
3. Enviar PDF pro cliente via WhatsApp
4. **PLUS:** Cancelamento de nota (endpoint já pronto na API!)

### Notas Importantes
- **Produto:** Manda a Nota (identidade própria, não WhatsApp genérico)
- Funcionou pro Helber mas não pro Eduardo inicialmente - correções aplicadas devem resolver para todos os usuários
- Endpoint de cancelamento já existe na API - se a API emite, também cancela

---

## 📱 Mobile App - Sessão da Tarde (17h)

### Problema: PDF Download no App
- App mostrava "PDF baixado com sucesso" mas depois erro "não é possível enviar mensagem vazia"
- PDF funciona no site mas não no app

### Princípio Importante (Helber)
> "O app deve ser um **espelho do site** — mesmos serviços, mesmos endpoints. O que funciona no site deve funcionar no app."

### Endpoints do Site (history.tsx) — App Deve Espelhar:
| Função | Endpoint | Método |
|--------|----------|--------|
| PDF | `/api/invoices/:id/pdf` | GET → blob |
| WhatsApp | `/api/invoices/:id/whatsapp-link` | GET → `{ link }` |
| Email | `/api/invoices/:id/send-email` | POST → `{ sendToTomador, sendToPrestador }` |
| Repetir | `/api/invoices` | POST → mesmos dados |
| Cancelar | `/api/invoices/:id/cancel` | POST → `{ motivo }` |

### Correções Aplicadas
1. Removido `expo-web-browser` que abria navegador externo
2. Reescrito `handleDownloadPDF` para usar `apiFetch` → blob → FileSystem → Share
3. Reescrito `handleShareWhatsApp` para chamar `/whatsapp-link` → `Linking.openURL`
4. Adicionado logs detalhados para debug do PDF

### Debug em Andamento
- Logs adicionados: `[PDF] Response status`, `[PDF] Blob size`, `[PDF] Share result`
- Aguardando teste do Helber para ver onde exatamente falha

### Arquivos Modificados
- `mobile-android/app/invoices.tsx` - Toda lógica de PDF/WhatsApp/Email/Repetir
